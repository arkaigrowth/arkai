# spec/ (ARKAI Spec Kernel)

Start at `spec/SYSTEM_MAP.md`.

## Principles
- JSON Schema Draft 2020-12 authored in YAML
- Modules exchange only schema-validated objects/events
- Specs may lead code, but must stay aligned via `spec/ALIGNMENT.md`

## Change control
Meaningful changes require a Safe Card (`spec/SAFE_CARD_TEMPLATE.yaml`).
