$schema: "https://json-schema.org/draft/2020-12/schema"
$id: "arkai://events/fact.packet.created"
title: "fact.packet.created"
type: object
allOf:
  - $ref: "../_base/event-envelope.yaml"
properties:
  schema_id:
    const: "arkai://events/fact.packet.created"
  payload:
    $ref: "../fact-packet.yaml"
